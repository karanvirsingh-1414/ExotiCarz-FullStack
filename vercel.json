{
  "buildCommand": "npm --prefix CarRental install && npm --prefix CarRental run build",
  "outputDirectory": "CarRental/dist",
  "installCommand": "npm --prefix CarRental install",
  "routes": [
    { "handle": "filesystem" },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
